# Скопируйте в `.env.local` (файл в корне, не коммитится).
# JWT_SECRET=длинная-случайная-строка
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="OmniNews <you@gmail.com>"
